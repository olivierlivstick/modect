[build]
  builder        = "DOCKERFILE"
  dockerfilePath = "apps/agent/Dockerfile"

[deploy]
  startCommand            = "node --import tsx/esm src/index.ts"
  restartPolicyType       = "ON_FAILURE"
  restartPolicyMaxRetries = 3
